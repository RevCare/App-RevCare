<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufrpe.revcare">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".infra.gui.HomeUsuario" />
        <activity android:name=".infra.gui.HomeProfissional" />
        <activity android:name=".infra.gui.CadastroProfissional1Activity" />
        <activity android:name=".infra.gui.CadastroUsuario1Activity" />
        <activity android:name=".infra.gui.MainActivity"/>
        <activity android:name=".infra.gui.ServicoActivity$ListaPratosActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>